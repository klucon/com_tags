# com_tags

Central tagging system for content and other extensions.

This repository contains a KLUCON CMS extension scaffold. Release packages are built from `src/**/<extension>/manifest.json` by GitHub Actions.
